function factorialize(num) {

  let result = 0;
  
  if(num === 0){
    return 1;
  }
  
  if(num < 0){
    return -1;
  }
  else{
    result = num * (factorialize(num-1));
  }
  
  return result;
}

factorialize(5);
