function mutation(arr) {
  
  let testElement = arr[0].toLowerCase();
  let targetElement = arr[1].toLowerCase();

  for(let i=0;i<targetElement.length;i++){
    if(testElement.indexOf(targetElement[i]) < 0){
      return false;
    }
  }
  return true;
}

mutation(["hello", "hey"]);
